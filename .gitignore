# ═══════════════════════════════════════════════════════════════
# BIZRA-DATA-LAKE .gitignore
# ═══════════════════════════════════════════════════════════════

# ── Pytest MagicMock leak artifacts ────────────────────────────
# Mock objects using __truediv__ (pathlib Path division) can
# escape to disk when state_dir is not properly patched.
*MagicMock*
nul
cleanup_mocks.py

# ── Secrets & Environment ─────────────────────────────────────
.env
.env.local
.env.*.local
.env.reference
.env.ref*
*.pem
*.pem.*
*.key
*.key.*
*.p12
*.pfx
*.csr

# ── Deploy runtime state ────────────────────────────────────
deploy/.deploy-version
deploy/.deploy-version.prev

# ── Python ────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
!frontend/src/lib/
!frontend/src/lib/**
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
pip-log.txt
pip-delete-this-directory.txt

# ── Virtual Environments ──────────────────────────────────────
.venv/
.venv-*/
venv/
env/
ENV/

# ── Jupyter ───────────────────────────────────────────────────
.ipynb_checkpoints
*-checkpoint.ipynb

# ── Node.js ───────────────────────────────────────────────────
node_modules/
npm-debug.log
yarn-error.log
yarn.lock
package-lock.json
!frontend/package-lock.json

# ── Rust Build Artifacts ──────────────────────────────────────
bizra-omega/target/
target/

# ── Legacy Workspaces (merged into bizra-omega/) ─────────────
native/
filedfs/mnt/
filedfs/*.so
filedfs/bizra-node-binary

# ── Data Pipeline (large generated data) ──────────────────────
00_INTAKE/
01_RAW/
02_PROCESSED/
03_INDEXED/
04_GOLD/
99_QUARANTINE/

# ── Reference PDFs (large, not source code) ──────────────────
*.pdf

# ── Large Binaries & Archives ─────────────────────────────────
*.zip
*.tgz
*.tar.gz
*.deb
*.rpm
*.dmg
*.iso
voices/
voices.tgz
dist.tgz
cuda-keyring_*.deb

# ── State & Checkpoints ──────────────────────────────────────
sovereign_state/
checkpoints/

# ── BIZRA Identity & Vault ───────────────────────────────────
.bizra/

# ── Claude / AI Tooling ──────────────────────────────────────
.claude-flow/
.swarm/
.claude/logs/
.claude/state/
.claude/settings.local.json

# ── IDE & Editors ─────────────────────────────────────────────
.vscode/
*.code-workspace
.idea/
*.swp
*~
*.bak

# ── OS Files ──────────────────────────────────────────────────
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.DS_Store
nul

# ── Test & Coverage ───────────────────────────────────────────
test_results/
coverage/
htmlcov/
.coverage
.pytest_cache/
.mypy_cache/
.tox/

# ── Logs ──────────────────────────────────────────────────────
*.log
*.tmp
logs/
missions/

# ── Compiled Binaries ─────────────────────────────────────────
# Root-anchored so only the top-level compiled `bizra` binary is
# ignored, not nested paths like `dema-console/src/components/bizra/`.
/bizra
*.exe

# ── ML Models (large files) ───────────────────────────────────
models/
*.gguf
*.bin
*.safetensors

# ── Research Archive (large files) ────────────────────────────
research_archive/

# ── Local Claude Config ───────────────────────────────────────
.claude/CLAUDE.local.md
.claude/agents/
!.claude/agents/pyo3-boundary-reviewer.md
.claude/commands/
.claude/helpers/
.claude/hooks/
.claude/memory/
.claude/rules/
.claude/skills/
!.claude/skills/bizra-node0-proactive-pilot/
!.claude/skills/bizra-node0-proactive-pilot/**
!.claude/skills/cross-lang-sync/
!.claude/skills/cross-lang-sync/**
!.claude/skills/data-pipeline-run/
!.claude/skills/data-pipeline-run/**
.claude/skills/**/__pycache__/
.claude/skills/**/*.py[cod]
.claude/statusline.*
# .mcp.json — tracked for team MCP server sharing

# ── Gemini ────────────────────────────────────────────────────
.gemini/

# ── Build / Benchmark Output ──────────────────────────────────
out/

# ── One-off Noise (untracked root artifacts) ─────────────────
mypy_full.txt
analyze_mypy.py

# ── Workspace Tarballs (ephemeral worktree artifacts) ────────
workspace-*.tar

# ── Archive (large, not source code) ─────────────────────────
BIZRA-Archive/

# ── Runtime workspace (separate Rust workspace, tracked independently) ──
runtime/.env
runtime/.env.reference
fix_mypy_batch.py
DATA4LLM_full_extract.txt

# ── Raw Agent / Session Dumps ────────────────────────────────
docs/SAPE_AGENT_FULL_OUTPUT.jsonl
**/memory.db

# ── Misc ──────────────────────────────────────────────────────
C:/

# Hypothesis test database
.hypothesis/

# ── Worktrees (Claude parallel checkouts) ─────────────────────
.claude/worktrees/

# ── Runtime SQLite State ──────────────────────────────────────
tmp_state/
users.db

# ── MagicMock / pytest file-leak artifacts ────────────────────
MagicMock*
*MagicMock*

# ── Audit Trail Data (generated at runtime) ───────────────────────
bizra-omega/bizra-node/data/audit/

# ── External Repos (submodules / clones) ───────────────────────
personaplex/
xtr-warp/

# ── Evidence Package Generated Outputs ─────────────────────────
artifacts/evidence/BIZRA-EVIDENCE-PACKAGE-v1.0-GENESIS/private_full/
artifacts/evidence/BIZRA-EVIDENCE-PACKAGE-v1.0-GENESIS/public_redacted/
artifacts/evidence/BIZRA-EVIDENCE-PACKAGE-v1.0-GENESIS/integrity/
artifacts/evidence/BIZRA-EVIDENCE-PACKAGE-v1.0-GENESIS/release/
artifacts/evidence/BIZRA-EVIDENCE-PACKAGE-v1.0-GENESIS/manifest/import_receipt.json

# Founding originals are source-locked from local vault paths; avoid accidental public commit
00_GENESIS/01_ARABIC_FOUNDING/*.pdf

# ── Worktree and tool output ──────────────────────────────────
BIZRA-DATA-LAKE/
.codeviz/

# ── Claude Code local state ──────────────────────────────────
.claude/logs/
.claude/memory/
.claude-flow/

# ── Misc build / runtime artifacts ───────────────────────────
.swarm/
tmp_state/
sovereign_state/
artifacts/phase56/
artifacts/atlas/workspace_masterpiece_report.json
artifacts/atlas/workspace_masterpiece_report.md
artifacts/atlas/
artifacts/inventory/
filefgfgs_extracted/
.tmp_prod_artifacts_v2/services/node_gateway/reflex_cache.sqlite

# ── Large artifacts (not tracked in git) ──────────────────────
99_ARCHIVE/
bizra_prod_artifacts_v2/
last update/
BIZRA-DDAGI-OS-Atlas-v5_0_FINAL.html
BIZRA-DDAGI-OS-Atlas-v5_0_FINAL_RESILIENT.html

# Local review / draft workspaces
tmp_review_probe*/
.spearpoint/
*.tsbuildinfo
BIZRA Front end final choese/
files with update/
my complete season/
BIZRA_DDAGI_OS_Deployable/
bizra-ddagi-os-frontend/
bizra-public/
BIZRA_DDAGI_OS_Complete.jsx
BIZRA_DDAGI_OS_Redesign.html
BIZRA_Production_Frontend.tsx
BIZRA_SovereignOS.jsx
BIZRA-Elite-Engineering-Blueprint.docx
BIZRA-Identity-Canon-v1.0.docx
BIZRA-Proof-Canon-v1.0.docx
BIZRA-Proof-Canon-v1.1.docx
BIZRA-Evidence-Delta-v1.0-to-v1.1.md
BIZRA-Sprint2-4-Execution-Playbook-v1.1.md

# Canon binaries should live under docs/canon, not terminal/
terminal/*.docx
vectors.db
preview-1.jpg
replace_script.py

# Linter/tool caches
.ruff_cache/
*.sarif
*.whl

# ── Corpus data (contains personal metadata) ───────────────
artifacts/corpus/

# ── Spearpoint runtime state (contains credentials) ──────────
artifacts/CANONICAL_SPEARPOINT_V1/_runtime_state/

# ── Security audit artifacts (credential reports) ─────────────
security_audit/
vendor/
04_GOLD/ruvector.db
04_GOLD/ruvector_bizra/
ruvector.db
bizra-omega/ruvector.db
.env
.claude/scheduled_tasks.lock
artifacts/proofs/loop-proof-*.json
