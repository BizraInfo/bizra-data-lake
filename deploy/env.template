# BIZRA Elite Framework — Environment Template
# ================================================
# SECURITY: Never commit actual secrets!
# Copy to .env and fill with real values
#
# Standing on Giants: 12-Factor App

# =============================================================================
# CORE CONFIGURATION
# =============================================================================
BIZRA_ENV=production
BIZRA_SECRET_KEY=<generate-with-secrets-manager>
BIZRA_NODE_ID=node0-genesis

# =============================================================================
# IHSAN THRESHOLDS (Constitutional)
# =============================================================================
IHSAN_THRESHOLD=0.95
SNR_DATA_THRESHOLD=0.90
SNR_INFORMATION_THRESHOLD=0.95
SNR_KNOWLEDGE_THRESHOLD=0.99
SNR_WISDOM_THRESHOLD=0.999

# =============================================================================
# MONITORING
# =============================================================================
GRAFANA_ADMIN_PASSWORD=<generate-secure-password>
PROMETHEUS_RETENTION_DAYS=30

# =============================================================================
# FEDERATION (Optional)
# =============================================================================
FEDERATION_ENABLED=false
FEDERATION_BOOTSTRAP_NODES=
FEDERATION_PORT=7777

# =============================================================================
# LLM BACKENDS
# =============================================================================
LLM_PRIMARY_HOST=192.168.56.1
LLM_PRIMARY_PORT=1234
LLM_FALLBACK_HOST=localhost
LLM_FALLBACK_PORT=11434

# =============================================================================
# DATABASE (Optional)
# =============================================================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=bizra
POSTGRES_USER=<db-user>
POSTGRES_PASSWORD=<db-password>

# =============================================================================
# REDIS (Optional)
# =============================================================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=<redis-password>
