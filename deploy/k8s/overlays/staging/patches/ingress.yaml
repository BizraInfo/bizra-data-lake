apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bizra-ingress
  namespace: bizra
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging
spec:
  tls:
    - hosts:
        - api.staging.bizra.node0
        - elite.staging.bizra.node0
      secretName: bizra-staging-tls
  rules:
    - host: api.staging.bizra.node0
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bizra-omega
                port:
                  number: 80
    - host: elite.staging.bizra.node0
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bizra-elite
                port:
                  number: 80
