{
  "_comment": "BIZRA Team MCP Configuration — checked into repo for team-wide consistency",
  "_version": "1.6.0",
  "_updated": "2026-04-21 — Repaired repo-local MCP defaults: use the repo venv for Python servers, switch sqlite to the local helper server, and remove the stale claude-flow entry. 9→8 servers.",

  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"],
      "description": "Live documentation lookup for libraries (tokio, pyo3, pydantic, fastapi, etc.)"
    },

    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory@latest"],
      "description": "Cross-session persistent memory"
    },

    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github@latest"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "GitHub issues, PRs, actions integration — reads GITHUB_TOKEN from .env"
    },

    "brave-search": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-brave-search@latest"],
      "env": {
        "BRAVE_API_KEY": "${BRAVE_API_KEY}"
      },
      "description": "Web search — requires BRAVE_API_KEY env var"
    },

    "sqlite": {
      "command": "bash",
      "args": ["-lc", "repo=$(git rev-parse --show-toplevel 2>/dev/null || pwd); cd \"$repo\" && exec \"$repo/.venv/bin/python\" \"$repo/tools/mcp/sqlite_mcp_server.py\" --db-path \"$repo/sovereign_state/living_memory/memory.db\" --server-name bizra-sqlite"],
      "description": "Structured query access to the repo-local living-memory SQLite database via the bundled read-only MCP server"
    },

    "bizra-sovereign": {
      "command": "bash",
      "args": ["-lc", "repo=$(git rev-parse --show-toplevel 2>/dev/null || pwd); cd \"$repo\" && exec \"$repo/.venv/bin/python\" \"$repo/tools/mcp/sovereign_mcp_server.py\" --stdio"],
      "description": "BIZRA Sovereign Engine MCP — constitutional gates, SNR, Ihsan"
    },

    "bizra-ecosystem": {
      "command": "bash",
      "args": ["-lc", "repo=$(git rev-parse --show-toplevel 2>/dev/null || pwd); cd \"$repo\" && exec \"$repo/.venv/bin/python\" \"$repo/tools/mcp/ecosystem_mcp_server.py\" --stdio"],
      "description": "BIZRA Ecosystem health, status, and monitoring"
    },

    "neo4j-cypher": {
      "command": "uvx",
      "args": ["mcp-neo4j-cypher"],
      "env": {
        "NEO4J_URL": "bolt://localhost:7687",
        "NEO4J_USERNAME": "neo4j",
        "NEO4J_PASSWORD": "${NEO4J_PASSWORD}",
        "NEO4J_DATABASE": "neo4j"
      },
      "description": "Neo4j Cypher graph queries — wisdom graph (bolt://localhost:7687)"
    }
  }
}
