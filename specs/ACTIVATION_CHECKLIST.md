# BIZRA Activation Checklist — From 45% to 100%

## Rule: No user invitation until ALL items are ✅

---

## CHAIN 1: Make the Agent DO Things (P0 — Blocks Everything)

```
1.1 → 1.2 → 1.3 → 1.4 → 1.5
```

### 1.1 FAISS Search in Mission Context
**When user sends a mission, agents search 84,795 vectors for relevant context.**
- Wire: `bizra-node RECEIVE` → query FAISS index → inject top-K chunks into prompt
- File: `bizra-omega/bizra-node/src/handler.rs` (RECEIVE handler)
- Test: `bizra mission "what did I work on last week"` returns REAL context
- [ ] TODO

### 1.2 File Management EXECUTES
**`bizra organize ~/Downloads` actually MOVES files.**
- Wire: File Management skill → filesystem operations → receipt per action
- Needs: Guardian approval before any move/delete
- File: `scripts/bizra` organize command → actual `mv`/`mkdir` operations
- Test: 5 files moved, receipt shows file paths
- [ ] TODO

### 1.3 Desktop Bridge EXECUTES
**`bizra` missions route through Desktop Bridge for real OS actions.**
- Wire: TUI mission → Desktop Bridge (port 9742) → execute_mission RPC
- File: `scripts/bizra` → `core/bridges/desktop_bridge.py` TCP call
- Test: `bizra mission "open my browser"` → browser actually opens
- [ ] TODO

### 1.4 MCP Tools Available in Missions
**Missions can use brave-search, filesystem, fetch MCP servers.**
- Wire: Mission prompt → detect tool need → invoke MCP server → include result
- File: Handler → MCP client → tool result → Ollama prompt enrichment
- Test: `bizra browse "latest AI news"` → real web results
- [ ] TODO

### 1.5 AHK Bridge for Windows Actions
**Windows-specific actions routed through AHK.**
- Wire: Desktop Bridge → AHK WebSocket → Windows automation
- Needs: AHK running (`bin/bizra_bridge.ahk`)
- Test: `Win+B` pings bridge, `Ctrl+B,Q` runs a query
- [ ] TODO

---

## CHAIN 2: Make the Agent KNOW You (P0 — Personality)

```
2.1 → 2.2 → 2.3
```

### 2.1 FAISS Query at Boot (Morning Briefing with REAL Data)
**Morning briefing pulls from 84,795 vectors, not empty memory.**
- Wire: `LivingMemory.load()` → FAISS query for recent topics → inject into briefing
- File: `core/living_memory/brain.py` → `generate_morning_briefing()`
- Test: Briefing mentions actual projects/topics from your corpus
- [ ] TODO

### 2.2 Mission History Persisted + Displayed
**Wallet shows REAL mission count, REAL SEED earned, REAL streak.**
- Wire: After each mission → update `user_model.json` → wallet reads it
- File: `core/living_memory/brain.py` → `update_after_mission()` called from TUI
- Test: Complete 5 missions → wallet shows 5 missions, ~10-15 SEED, streak 5
- [ ] TODO

### 2.3 Reflex Compilation Visible
**Patterns compile into reflexes after 5 repetitions.**
- Wire: Heartbeat → check pattern count → compile → display in wallet
- File: `bizra-agent/reflex_compiler.rs` → wallet display
- Test: Do same task 5 times → "⚡ REFLEX COMPILED" notification
- [ ] TODO

---

## CHAIN 3: Make the Agent PROACTIVE (P1 — Differentiator)

```
3.1 → 3.2 → 3.3
```

### 3.1 Ghost Panel with REAL Ollama Inference
**Proactive suggestions generated by actual LLM, not random selection.**
- Wire: Ghost Panel → Ollama inference with user context → real suggestion
- File: `scripts/bizra` ghost command → Ollama call with memory context
- Test: Ghost suggests something RELEVANT to your current work
- [ ] TODO

### 3.2 Proactive Mode: Suggest
**Agent observes filesystem changes and suggests actions.**
- Wire: Heartbeat → scan Downloads for new files → suggest organization
- File: Proactive engine (new) → filesystem watcher → suggestion display
- Test: Drop a file in Downloads → ghost suggests organizing it
- [ ] TODO

### 3.3 Proactive Mode: Auto-Execute Pre-Approved
**Agent auto-executes pre-approved actions (backup, organize, summarize).**
- Wire: Proactive engine → pre-approved list → execute → receipt
- Needs: User confirms auto-approve rules at setup
- Test: New PDF in Downloads → auto-moved to Documents/PDFs/
- [ ] TODO

---

## CHAIN 4: Make the Network LIVE (P1 — Multi-Node)

```
4.1 → 4.2 → 4.3
```

### 4.1 Federation Gossip Listener
**NODE0 listens for incoming peer connections.**
- Wire: `bizra start` → FederationNode.start() → gossip listener on :9750
- File: `bizra-omega/bizra-node/src/node.rs` → tokio TCP listener
- Test: `netstat -tlnp | grep 9750` shows listener
- [ ] TODO

### 4.2 Cross-Node Receipt Verification
**Node B can verify Node A's receipt chain.**
- Wire: Gossip → share receipt hashes → verify_chain() cross-node
- File: `bizra-federation/attestation.rs` + `bizra-mission/receipt.rs`
- Test: Two nodes, Node B verifies Node A's 10-receipt chain
- [ ] TODO

### 4.3 Telescript Cross-Node Mission
**A mission travels from Node A to Node B and returns results.**
- Wire: Telescript → Federation gossip → remote execute → result back
- File: `bizra-telescript/` → `bizra-federation/` → A2A transport
- Test: Node A asks Node B to research a topic → receipt from both nodes
- [ ] TODO

---

## CHAIN 5: Make the Economy REAL (P2 — Sustainability)

```
5.1 → 5.2 → 5.3
```

### 5.1 SEED Balance Persistence
**SEED earned persists across sessions, shown in wallet.**
- Wire: PoI SEED calc → append to `~/.bizra/seed_ledger.jsonl` → wallet reads
- Test: Earn SEED → restart → wallet shows same balance
- [ ] TODO

### 5.2 Skill Marketplace (Install New Skills)
**`bizra install-skill <url>` downloads and activates a skill.**
- Wire: Skill manifest → download → validate → install to `.claude/skills/`
- Test: Install a community skill → new command appears in `skills`
- [ ] TODO

### 5.3 SEED Transfer Between Nodes
**Send SEED to another node via Telescript Mudarabah contract.**
- Wire: Telescript → ExactAmount + BoundedRatio → cross-node settlement
- Test: Node A sends 10 SEED to Node B → both balances update
- [ ] TODO

---

## CHAIN 6: Make the Frontend LIVE (P2 — Visual)

```
6.1 → 6.2
```

### 6.1 Frontend Connects to Live Kernel
**React dashboard shows real data from kernel daemon API.**
- Wire: `npm run dev` → proxy :9740 → live health, agents, receipts
- Test: Dashboard shows real Ihsan score, agent status, receipt chain
- [ ] TODO

### 6.2 Operator Cockpit Shows Pipeline
**Constitutional pipeline visualization: Intent → Guardian → Execution → Receipt.**
- Wire: Cockpit component → WebSocket → real-time mission flow
- Test: Run mission in TUI → Cockpit shows pipeline stages in real-time
- [ ] TODO

---

## Completion Gate

```
CHAIN 1 (DO things):      0/5  ███░░░░░░░
CHAIN 2 (KNOW you):       0/3  ███░░░░░░░
CHAIN 3 (PROACTIVE):      0/3  ███░░░░░░░
CHAIN 4 (NETWORK):        0/3  ███░░░░░░░
CHAIN 5 (ECONOMY):        0/3  ███░░░░░░░
CHAIN 6 (FRONTEND):       0/2  ███░░░░░░░
─────────────────────────────────────────
TOTAL:                     0/19

INVITE GATE: 19/19 = ✅ INVITE USERS
             <19   = ❌ NOT READY
```

**Priority order: Chain 1 → Chain 2 → Chain 3 → Chain 4 → Chain 5 → Chain 6**

Chain 1 is the wall. If the agent can't DO things, nothing else matters.
