# Security Hooks (v0.1)

This kit includes optional wrappers. For best results install:
- `trufflehog` (secrets scanning)
- language-specific dependency scanners (cargo-audit, npm audit, pip-audit)

You can expand this into a full threat model + pen-test schedule later.
