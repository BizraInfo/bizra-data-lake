// BIZRA Operator Cockpit — Sprint 3 Product Surface
// Constitutional pipeline view: Intent → Guardian → Execution → Receipt → Chain → Evidence
// Uses existing hooks: useSovereignHealth, useSeedPotential, useTokenBalance
// Uses existing manifest: PAT_AGENT_MANIFEST, SAT_AGENT_MANIFEST, TERMINAL_THEME
export { default } from './OperatorCockpit.impl';
