{
  "_comment": "BIZRA Team MCP Configuration — checked into repo for team-wide consistency",
  "_version": "1.4.0",
  "_updated": "2026-04-07 — Added neo4j-cypher MCP server (12 servers total)",

  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"],
      "description": "Live documentation lookup for libraries (tokio, pyo3, pydantic, fastapi, etc.)"
    },

    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem@latest", "."],
      "description": "Local filesystem access for the repo"
    },

    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory@latest"],
      "description": "Cross-session persistent memory"
    },

    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking@latest"],
      "description": "Multi-step reasoning and problem decomposition"
    },

    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github@latest"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "GitHub issues, PRs, actions integration — reads GITHUB_TOKEN from .env"
    },

    "brave-search": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-brave-search@latest"],
      "env": {
        "BRAVE_API_KEY": "${BRAVE_API_KEY}"
      },
      "description": "Web search — requires BRAVE_API_KEY env var"
    },

    "sqlite": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sqlite@latest", "--db-path", "/mnt/c/BIZRA-DATA-LAKE/04_GOLD/bizra.db"],
      "description": "Structured query access to the primary BIZRA SQLite knowledge base"
    },

    "claude-flow-sqlite": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sqlite@latest", "--db-path", "/mnt/c/BIZRA-DATA-LAKE/.swarm/memory.db"],
      "description": "Structured query access to Claude-Flow memory.db for sessions, patterns, and memory entries"
    },

    "bizra-sovereign": {
      "command": "python",
      "args": ["tools/mcp/sovereign_mcp_server.py", "--stdio"],
      "description": "BIZRA Sovereign Engine MCP — constitutional gates, SNR, Ihsan"
    },

    "bizra-ecosystem": {
      "command": "python",
      "args": ["tools/mcp/ecosystem_mcp_server.py", "--stdio"],
      "description": "BIZRA Ecosystem health, status, and monitoring"
    },

    "neo4j-cypher": {
      "command": "uvx",
      "args": ["mcp-neo4j-cypher"],
      "env": {
        "NEO4J_URL": "bolt://localhost:7687",
        "NEO4J_USERNAME": "neo4j",
        "NEO4J_PASSWORD": "${NEO4J_PASSWORD}",
        "NEO4J_DATABASE": "neo4j"
      },
      "description": "Neo4j Cypher graph queries — wisdom graph (bolt://localhost:7687)"
    },

    "claude-flow": {
      "command": "node",
      "args": ["/usr/lib/node_modules/claude-flow/bin/cli.js", "mcp", "start", "--json"],
      "cwd": "/mnt/c/BIZRA-DATA-LAKE",
      "env": {
        "CLAUDE_FLOW_CONFIG": "/mnt/c/BIZRA-DATA-LAKE/.claude-flow/config.yaml",
        "BIZRA_DATA_LAKE": "/mnt/c/BIZRA-DATA-LAKE",
        "REDIS_URL": "redis://127.0.0.1:6379",
        "REDIS_SYNAPSE_URL": "redis://127.0.0.1:6380",
        "OLLAMA_HOST": "http://127.0.0.1:11434"
      },
      "description": "ruflo/claude-flow v3.5.58 — swarm orchestration, HNSW memory, Q-learning routing, 40+ MCP tools"
    }
  }
}
