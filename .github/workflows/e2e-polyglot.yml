name: E2E Polyglot (Cycle-6 G4)

# Intentionally red until Cycle-6 G4 closes.
# The red status is the visible pressure gauge for an open gate.
# See scripts/e2e-polyglot/README.md — "Why red now, by design".

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  e2e-polyglot:
    name: Cycle-6 G4 polyglot smoke
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - name: Run E2E polyglot smoke test
        run: bash scripts/e2e-polyglot/test.sh
